{
  "name": "simplr-gulp",
  "version": "2.0.0-alpha.1",
  "private": true,
  "author": "QuatroDev",
  "scripts": {
    "typings-init": "typings install",
    "compile-temp": "tsc -p . -outDir _tmp",
    "test": "npm run typings-init && npm run compile-temp && RMDIR _tmp /S /Q && echo Test completed.",
    "test-linux": "npm run typings-init && npm run compile-temp && rm -rf _tmp && echo Test completed."
  },
  "devDependencies": {
    "rollup": "0.34.1",
    "rollup-plugin-typescript": "0.7.7",
    "rollup-watch": "2.5.0",
    "tslint": "3.13.0",
    "typescript": "2.0.0",
    "typings": "1.3.2"
  }
}
